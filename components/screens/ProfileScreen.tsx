import { View, Text } from 'react-native'
import React from 'react'
import { TText } from '../../themed/themeComponents'

const ProfileScreen = React.memo((props:any) => {
  return (
    <View>
      <TText>ProfileScreen</TText>
    </View>
  )
})

export default ProfileScreen