import { View, Text } from 'react-native'
import React from 'react'
import { TText } from '../../themed/themeComponents'

const FriendsScreen = React.memo( (props: any) => {
  return (
    <View>
      <TText>Home Screen</TText>
    </View>
  )
})

export default FriendsScreen